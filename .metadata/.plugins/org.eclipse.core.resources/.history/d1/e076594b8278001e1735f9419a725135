#include "pcb_allocator.h"

typedef struct{
	int Pid;
	int Pc;
	int Reg[16];
	int Cpsr;
}PCB;

typedef struct PCB_NODE{
	PCB pcb_data;
	struct PCB_NODE * next;
}PCB_NODE;


void allocate_pcb(PCB data){
	PCB_NODE * node = (PCB_NODE*)malloc(sizeof(PCB_NODE));
	if (node == NULL){

	}
}
